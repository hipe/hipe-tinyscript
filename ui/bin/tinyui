#!/usr/bin/env ruby

root = File.expand_path('../../..', File.dirname(__FILE__))
$LOAD_PATH.unshift(root) unless $LOAD_PATH.include?(root)

require 'hipe-tinyscript/ui/app'

Hipe::Tinyscript::Ui::App.new.run(ARGV)
